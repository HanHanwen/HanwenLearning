判断

```shell
-eq #equal，数字进行判断是否相等
-ne #inequality，数字判断不等于

-lt #less then，数字进行判断是否小于
-gt #greter then，数字进行判断是否大于
-ge #greter equal，数字判断大于等于
-le #less equal，数字判断小于等于
```

```sh
==  #用户字符串判断，与“=”功能一致
-a  #and，与“&&”功能一致
-o  #or，与“||”功能一致
```





变量替换